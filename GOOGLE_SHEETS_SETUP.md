# บันทึกผลเกมลง Google Sheets

เกมจะบันทึกผลเมื่อผู้เล่นทำครบ 10 ภารกิจ โดยส่งเวลา ชื่อ ชั้น เลขที่ คะแนน และจำนวนภารกิจที่ได้ 0 คะแนนไปยังชีต `Results`.

## ตั้งค่าครั้งเดียว

1. สร้าง Google Sheet ใหม่สำหรับเก็บผล และตั้งชื่อ เช่น `ผลการเล่นนักสืบแยกสาร`.
2. ในชีตนั้น เลือก **Extensions > Apps Script**.
3. แทนที่โค้ดเริ่มต้นทั้งหมดด้วยเนื้อหาจาก `google-apps-script/Code.gs` ในโครงการนี้ แล้วกดบันทึก.
4. ใน Apps Script ให้เลือกฟังก์ชัน `setupResultsSheet` แล้วกด **Run** หนึ่งครั้ง เพื่ออนุญาตสิทธิ์และสร้างแท็บ `Results` พร้อมหัวตาราง.
5. เลือก **Deploy > New deployment > Web app**.
   - **Execute as:** Me
   - **Who has access:** เลือกตัวเลือกที่ทำให้ผู้เล่นเกมเข้าถึงได้ (สำหรับเว็บเกมสาธารณะมักเป็น `Anyone`)
6. กด **Deploy** และอนุญาตสิทธิ์ตามที่ Google ขอ จากนั้นคัดลอก URL ที่ลงท้ายด้วย `/exec`.
7. เปิด `index.html` แล้ววาง URL ในบรรทัดนี้:

   ```js
   const GOOGLE_SHEETS_WEB_APP_URL = '';
   ```

   ตัวอย่าง:

   ```js
   const GOOGLE_SHEETS_WEB_APP_URL = 'https://script.google.com/macros/s/…/exec';
   ```

8. อัปโหลดหรือ push `index.html` เวอร์ชันใหม่นี้ แล้วลองเล่นจนจบหนึ่งรอบ. แถวใหม่ควรปรากฏในแท็บ `Results`.

## ข้อควรทราบ

- ใช้ URL `/exec` สำหรับเกมจริง; URL `/dev` ใช้ทดสอบได้เฉพาะผู้ที่แก้ไข Apps Script ได้.
- เก็บ Google Sheet ไว้เฉพาะครูหรือผู้รับผิดชอบ และเก็บข้อมูลผู้เรียนเท่าที่จำเป็น.
- URL ของ Web App อยู่ใน JavaScript สาธารณะ จึงไม่ใช่รหัสลับ. หากต้องจำกัดผู้ส่งผลจริง ๆ ต้องมีระบบยืนยันตัวตนเพิ่มเติม.
